{
  "name": "$PATTERN_NAME",
  "services": [
    {
      "serviceUrl": "$SERVICE_CONTAINER",
      "serviceOrgid": "$DOCKER_HUB_ID",
      "serviceArch": "$ARCH",
      "serviceVersions": [ { "version": "$SERVICE_VERSION" } ]
    }
  ]
}

